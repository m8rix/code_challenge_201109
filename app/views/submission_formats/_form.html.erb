<%= form_with(model: submission_format, local: true) do |form| %>
  <% if submission_format.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(submission_format.errors.count, "error") %> prohibited this submission_format from being saved:</h2>

      <ul>
        <% submission_format.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
